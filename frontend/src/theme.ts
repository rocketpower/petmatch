import { createMuiTheme }  from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: { 
      light: '#C969B0',
      main: '#7D1B63',
      dark: '#4A103B',
      contrastText: '#FFF'
    }
  },
});

export default theme